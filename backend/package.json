{
  "name": "mamai-backend",
  "version": "1.0.0",
  "type": "module",
"scripts": {
  "dev": "nodemon server.js",
  "start": "node server.js",
  "check-python": "node -e \"const http=require('http'); http.get('http://localhost:5001/health', (r)=>console.log('✅ Python server running')).on('error', ()=>console.log('❌ Python server not running'))\""
},

  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}

